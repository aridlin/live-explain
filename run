#!/bin/sh
set -eu
cd "$(dirname "$(readlink -f "$0")")"
if [ ! -x .venv/bin/live-explain ]; then
    printf '%s\n' 'Run uv sync --frozen once to install dependencies.' >&2
    exit 1
fi
exec .venv/bin/live-explain "$@"
