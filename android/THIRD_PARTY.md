# Android dependencies

The production app uses native Android platform APIs and has no bundled third-party runtime dependencies.
AndroidX Test Runner / Rules (Apache 2.0) and JUnit (EPL 1.0) are test-only dependencies.
ZXing and AndroidX Core were removed in 0.3.0 along with camera and QR pairing.
